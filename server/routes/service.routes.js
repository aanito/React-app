// const express = require('express');
// const router = express.Router();
// const Service = require('../models/ServiceModel');

// router.get('/api/services', async (req, res) => {
//   try {
//     const services = await Service.find(); // Fetch all services from the 'services' collection
//     res.json(services); // Send the fetched services as a JSON response
//   } catch (error) {
//     console.error('Error fetching services:', error);
//     res.status(500).json({ error: 'An error occurred while fetching services' });
//   }
// });

// module.exports = router;
